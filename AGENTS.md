# Agents

This file contains configuration for AI agents working with this repository.

## Agent skills

### Issue tracker

Issues live in the repo's GitHub Issues (uses the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout with one `CONTEXT.md` at the repo root. See `docs/agents/domain.md`.